
const ButtonX = ({onClick} ) => {
  return (
    <button
      onClick={onClick}
      className=" text-black bg-white button p-1 cursor-pointer"
    >
      X
    </button>
  );
};

export default ButtonX